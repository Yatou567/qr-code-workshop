{
  "name": "qr-code-workshop",
  "version": "1.0.0",
  "private": true,
  "description": "Offline QR code generator and Chrome extension for webpage links",
  "license": "MIT",
  "engines": {"node": ">=20"},
  "scripts": {"test": "node --test tests/*.test.cjs", "build": "node scripts/build.cjs"}
}
