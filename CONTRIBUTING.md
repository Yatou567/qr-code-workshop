# 贡献说明

欢迎提交中文或英文 Issue / Pull Request。

1. 描述问题、复现步骤、预期结果及 Chrome / 操作系统版本。
2. 修改前先运行 `npm test`，修改后再执行 `npm test` 和 `npm run build`。
3. 保持本地生成、无需服务端的设计；新增权限应说明必要性。
4. 对网页提供的文字使用 `textContent` 等安全接口，避免将网页内容作为 HTML 执行。
5. 不提交个人网址、访问令牌、账号资料、浏览记录或包含敏感内容的截图。
6. 涉及交互或页面样式时，按照 `docs/testing.md` 完成真实浏览器检查并说明结果。

## 发布

维护者先更新扩展版本及 CHANGELOG，运行测试和打包，再将 `dist/` 中的文件附加到对应版本的 GitHub Release。请勿把未经验证的测试结果写成已通过。
